PROMPT CURSOR - DEBUG PRODUITS / COMMANDES (CLIENT / SERVEUR / CUISINE)

Je travaille sur une application web (Frontend + Backend) de gestion des produits et commandes pour un restaurant.

CONTEXTE
- Le client voit les produits et clique pour commander.
- La section "Commande du client" doit afficher les produits ajoutés.
- Un serveur peut aussi enregistrer et valider une commande.
- Une commande validée doit être visible côté cuisine ("A preparer").
- Chaque commande doit avoir : Modifier / Supprimer / Valider.

PROBLEMES ACTUELS
1) `image_url` ne s'affiche pas (images absentes).
2) La commande du client reste vide apres clic sur un produit.
3) Les boutons Modifier / Supprimer / Valider n'apparaissent pas.
4) Erreur terminal :
   `C:\Users\LENOVO\.cursor\projects\c-Users-LENOVO-Documents-examens\terminals\3.txt` lignes 138-139
   (403 Forbidden sur GET /api/v1/orders).

OBJECTIFS
- Afficher correctement les images produits via `image_url`.
- Ajouter automatiquement un produit a la commande au clic.
- Afficher la commande du client en temps reel.
- Activer Modifier / Supprimer / Valider pour chaque commande.
- Quand une commande est validee, elle doit apparaitre cote cuisine.

POINTS A VERIFIER OBLIGATOIREMENT
- Backend ou frontend : localiser la cause exacte.
- API produits : verifier presence de `image_url` dans le JSON (et non `imageUrl`).
- Frontend : verifier mapping de cle, balise `<img src="...">`, URL complete, erreurs 404/CORS.
- Commandes : verifier appel d'ajout (POST), mise a jour du state frontend, et affichage de la liste.
- API commandes : verifier routes GET/POST/PUT-PATCH/DELETE (lire, creer, modifier, valider, supprimer).
- Auth/permissions : expliquer et corriger le 403 sur `/api/v1/orders`.
- Base de donnees : verifier champ `status` (`pending`, `validated`, `preparing`, `done`).
- Cuisine : afficher uniquement les commandes validees.

CE QUE TU DOIS FOURNIR
1) Cause exacte de chaque bug.
2) Corrections completes (frontend + backend).
3) Fichiers modifies + lignes changees.
4) Code final fonctionnel et explication claire de chaque correction.

DONNEES A ANALYSER (A COMPLETER SI NECESSAIRE)
- Code autour des lignes 138-139.
- JSON reponse API produits.
- JSON reponse API commandes.
- Erreurs console navigateur.
